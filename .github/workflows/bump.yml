name: Bump helium-chromium submodule to latest version

on: workflow_dispatch

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with: { submodules: true, fetch-depth: 0 }

      - name: Run bump-platform action from helium-chromium
        uses: thenhnn/helium-chromium/.github/actions/bump-platform@nhnn/vertical-tabs-142
        with:
          token: ${{ secrets.GHPAT }}
